# Yeaster — Python runtime dependencies.
# Install:  python -m venv venv && venv/bin/pip install -r requirements.txt
# (The web control room is separate — see web/package.json.)
# LLM calls go over plain HTTPS via `requests`, so no openai/anthropic SDK is needed.

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
requests>=2.31

# ── Dev / tests (optional) ───────────────────────────────────────────────────
# Run the suite with:  venv/bin/pip install -r requirements.txt && venv/bin/pytest
pytest>=8.0
httpx>=0.27
