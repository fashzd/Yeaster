"""Yeaster — an autonomous BNB momentum trading agent.

One agent, one wallet. Its cognition runs as sequential reasoning passes of a
single mind — ``screen → grade → vet → commit`` — driven by a deterministic
cycle runner, wrapped in a non-bypassable safety firewall and a tamper-evident
proof ledger.
"""

__version__ = "0.1.0"
